package hotel;

import java.util.ArrayList;
import java.util.List;

public class Application {
	
	private ArrayList<Hotel> listeDesHotels = new ArrayList<>();

	public void demanderInformationsReservation(Reservation r) {
	    /*// Demandez les informations de réservation à l'utilisateur
	    String nom = /* Saisie du nom /;
	    String prenom = / Saisie du prénom /;
	    String numeroCarteCredit = / Saisie du numéro de carte de crédit /;
	    String dateExpirationCarte = / Saisie de la date d'expiration /;
	    String codeSecuriteCarte = / Saisie du code de sécurité /;

	    // Créez un objet Client avec les informations saisies
	    Client client = new Client(nom, prenom, new CarteCredit(numeroCarteCredit, dateExpirationCarte, codeSecuriteCarte));

	    // Créez un objet Chambre pour la réservation (peut-être basé sur le choix de l'utilisateur)
	    Chambre chambreAReserver = / Sélection de la chambre à réserver */;

	    // Créez un objet Reservation
	    //Reservation reservation = new Reservation(client, hotel, dateArrivee, dateDepart, chambreAReserver);

	    // Stockez la réservation ou effectuez d'autres opérations nécessaires
	}
	
}
